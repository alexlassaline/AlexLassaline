ML_Code uses a predicative machine learning model to trade commodities
yfinance_data pulls info from yahoo finance into a csv

This took way too long to code

Ensure your directories are modified to fit directions in code

The following API imports are required, if you don't have them here are the pips to run in CMD:

pip install yfinance
pip install pandas
pip install numpy
pip install scikit-learn
pip install matplotlib
